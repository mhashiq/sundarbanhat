// global PRODUCT_DB
window.PRODUCT_DB = {
    "honey-kholisha": {
        "title": "সুন্দরবনের খাঁটি খলিশা মধু",
        "subcategory": "সুন্দরবনের খাঁটি মধু",
        "category": "honey",
        "price": "৳৮৫০",
        "weight": "৫০০ গ্রাম",
        "location": "গহীন সুন্দরবন",
        "harvest": "মে ২০২৬",
        "story": "মৌয়ালদের মাধ্যমে বাঘের রাজত্ব সুন্দরবনের গভীর ম্যানগ্রোভ থেকে সরাসরি সংগৃহীত খলিশা ফুলের চাক ভাঙ্গা মধু। এটি সম্পূর্ণ প্রাকৃতিক ও ভেজালমুক্ত, যা আপনার রোগ প্রতিরোধ ক্ষমতা এবং শারীরিক শক্তি বৃদ্ধিতে সাহায্য করবে। খলিশা মধুর বিশেষ সোনালী রঙ এবং হালকা মিষ্টি সুগন্ধ থাকে যা এটিকে অত্যন্ত প্রিমিয়াম করে তোলে।",
        "benefits": [
            "১০০% খাঁটি ও সুন্দরবনের আসল খলিশা মধু।",
            "রোগ প্রতিরোধ ক্ষমতা ও শারীরিক এনার্জি বাড়াতে অত্যন্ত কার্যকর।",
            "শ্বাসকষ্ট, সর্দি ও কাশির নিরাময়ে অত্যন্ত সহায়ক।",
            "কৃত্রিম চিনি, প্রিজারভেটিভ বা ফ্লেভার মুক্ত শতভাগ প্রাকৃতিক।"
        ],
        "storage": "সাধারণ তাপমাত্রায় কাঁচের বোতলে মুখ ভালো করে আটকে শুষ্ক স্থানে সংরক্ষণ করুন। ফ্রিজে রাখার প্রয়োজন নেই। রোদ থেকে দূরে রাখুন।",
        "img": "honey-box.png"
    },
    "honey-goran": {
        "title": "সুন্দরবনের গরান ফুলের মধু",
        "subcategory": "সুন্দরবনের খাঁটি মধু",
        "category": "honey",
        "price": "৳৯০০",
        "weight": "৫০০ গ্রাম",
        "location": "গহীন সুন্দরবন",
        "harvest": "এপ্রিল ২০২৬",
        "story": "সুন্দরবনের গরান গাছের ফুল থেকে সংগৃহীত লালচে রঙের কড়া মিষ্টি স্বাদের মধু। খলিশা মধুর তুলনায় এটি কিছুটা ঘন ও এর সুগন্ধ অত্যন্ত তীব্র ও আকর্ষণীয়। গরান মধু উচ্চ মানের অ্যান্টি-অক্সিডেন্ট সমৃদ্ধ যা বার্ধক্য রোধ করতে এবং কোষের সুরক্ষা দিতে কাজ করে।",
        "benefits": [
            "রক্ত পরিষ্কার করতে এবং হজম ক্ষমতা বাড়াতে সাহায্য করে।",
            "ত্বকের উজ্জ্বলতা বৃদ্ধিতে ও দ্রুত ক্ষত নিরাময়ে উপকারী।",
            "উচ্চ অ্যান্টি-অক্সিডেন্ট সমৃদ্ধ খাঁটি র মধু।"
        ],
        "storage": "বোতলের ছিপি ভালো করে আটকে রাখুন। পিঁপড়ার আক্রমণ থেকে বাঁচতে শুষ্ক স্থানে রাখুন। ফ্রিজে রাখবেন না।",
        "img": "comingsoon.png"
    },
    "prawn-premium": {
        "title": "প্রিমিয়াম বাগদা চিংড়ি",
        "subcategory": "তাজা মাছ ও চিংড়ি",
        "category": "fish",
        "price": "৳৯০০",
        "weight": "৫০০ গ্রাম",
        "location": "শ্যামনগর উপকূলীয় ঘের",
        "harvest": "জুলাই ২০২৬",
        "story": "শ্যামনগরের লবণাক্ত পানির প্রাকৃতিক ঘের থেকে প্রতিদিন ভোরে সংগৃহীত তাজা বাগদা চিংড়ি। কোনো কৃত্রিম কেমিক্যাল বা প্রিজারভেটিভ ছাড়া সম্পূর্ণ প্রাকৃতিক শৈবাল ও প্ল্যাঙ্কটন খেয়ে বড় হওয়া তাজা চিংড়ি। আমাদের চিংড়িগুলো মিষ্টি লোনা পানির কারণে অত্যন্ত সুস্বাদু ও পুষ্টিকর হয়ে থাকে।",
        "benefits": [
            "সরাসরি শ্যামনগরের লোনা ঘের থেকে সতেজ আহরণ ও কুরিয়ার।",
            "১০০% প্রিজারভেটিভ, ফরমালিন এবং ইউরিয়া মুক্ত।",
            "অত্যন্ত নরম, সুস্বাদু ও ঘিলু সমৃদ্ধ প্রিমিয়াম চিংড়ি।"
        ],
        "storage": "পণ্যটি হাতে পাওয়ার সাথে সাথে ডিপ ফ্রিজে সংরক্ষণ করুন। রান্নার কিছুক্ষণ আগে ফ্রিজ থেকে বের করে ধুয়ে নিন।",
        "img": "prawn.jpg"
    },
    "prawn-golda": {
        "title": "প্রিমিয়াম গলদা চিংড়ি",
        "subcategory": "তাজা মাছ ও চিংড়ি",
        "category": "fish",
        "price": "৳১০০০",
        "weight": "৫০০ গ্রাম",
        "location": "সাতক্ষীরা নদী সংলগ্ন মিষ্টি ঘের",
        "harvest": "জুলাই ২০২৬",
        "story": "সাতক্ষীরার মিষ্টি ও আধা-লোনা জলের নদী ও সংলগ্ন ঘের থেকে সংগৃহীত বড় আকারের গলদা চিংড়ি। এর নরম মাংস ও মাথার ভেতরের পুষ্টিকর হলুদ ঘিলু তরকারির স্বাদ বহুগুণ বাড়িয়ে দেয়। আমরা সরাসরি প্রান্তিক ঘের মালিকদের থেকে এটি সংগ্রহ করি।",
        "benefits": [
            "প্রাকৃতিক ও নিরাপদ চাষ পদ্ধতিতে বড় হওয়া চিংড়ি।",
            "উচ্চ মানের ওমেগা-৩ ও খনিজ উপাদান সমৃদ্ধ।",
            "ক্যালসিয়াম ও ভিটামিন বি-১২ এর চমৎকার প্রাকৃতিক উৎস।"
        ],
        "storage": "ডিপ ফ্রিজে মাইনাস তাপমাত্রায় দীর্ঘদিন সংরক্ষণ করা সম্ভব।",
        "img": "comingsoon.png"
    },
    "shutki-prawn": {
        "title": "চিংড়ি শুঁটকি (হাতে কাটা)",
        "subcategory": "শুঁটকি ও সামুদ্রিক পণ্য",
        "category": "fish",
        "price": "৳৮০০",
        "weight": "৫০০ গ্রাম",
        "location": "শ্যামনগর Hub",
        "harvest": "জুন ২০২৬",
        "story": "উপকূলের তাজা বাগদা চিংড়ি ভালো করে ধুয়ে খোলস ও মাথা ছাড়িয়ে হাতে কেটে রোদে শুকিয়ে প্রস্তুত করা অসাধারণ চিংড়ি শুঁটকি। শুঁটকিটি সম্পূর্ণ শুকানো ও বালুহীন, যাতে কুরিয়ারের পর দীর্ঘদিন ভালো থাকে। এতে কোনো কীটনাশক বা রাসায়নিক দেওয়া হয় না।",
        "benefits": [
            "১০০% প্রাকৃতিকভাবে রোদে শুকানো পরিষ্কার শুঁটকি।",
            "কোনো ক্ষতিকর প্রিজারভেটিভ বা অতিরিক্ত লবণ নেই।",
            "ভর্তা ও ডাল-সবজি রান্নায় দেয় অসাধারণ মিষ্টি স্বাদ।"
        ],
        "storage": "বায়ুরোধী বয়ামে ভরে নরমাল ফ্রিজে রেখে ৫-৬ মাস সংরক্ষণ করতে পারেন।",
        "img": "comingsoon.png"
    },
    "mango-rupali": {
        "title": "সাতক্ষীরার বিখ্যাত আম (রূপালী)",
        "subcategory": "মৌসুমি ফল",
        "category": "fruit",
        "price": "৳৪৫০",
        "weight": "৫ কেজি",
        "location": "দেবহাটা আম বাগান, সাতক্ষীরা",
        "harvest": "জুন ২০২৬",
        "story": "ফরমালিনমুক্ত, সরাসরি দেবহাটার বাগান থেকে সংগৃহীত সুমিষ্ট রূপালী আম। রাসায়নিক কার্বাইড ছাড়াই গাছপাকা অত্যন্ত রসালো ও সুস্বাদু আম। সাতক্ষীরার মাটি ও আবহাওয়ার কারণে এখানকার আম সারা দেশের মধ্যে সবার আগে পাকে এবং স্বাদে অনন্য মিষ্টি হয়ে থাকে।",
        "benefits": [
            "সরাসরি সাতক্ষীরার আম বাগান থেকে সংগৃহীত ও প্যাকিং।",
            "কার্বাইড, ইথ্রেল ও ফরমালিন মুক্ত সম্পূর্ণ নিরাপদ আম।",
            "সুমিষ্ট ও রসালো স্বাদের প্রিমিয়াম আম।"
        ],
        "storage": "পণ্য পৌঁছানোর পর কার্টন থেকে বের করে ঠাণ্ডা ও শুষ্ক স্থানে ছড়িয়ে রাখুন। আম সম্পূর্ণ পেকে নরম হলে ফ্রিজে রাখতে পারেন।",
        "img": "mango.JPG"
    },
    "mustard-oil": {
        "title": "কাঠের ঘানির সরিষার তেল",
        "subcategory": "খাঁটি তেল ও শস্য",
        "category": "oil",
        "price": "৳৩৫০",
        "weight": "১ লিটার",
        "location": "আশাশুনি সরিষা মিল, সাতক্ষীরা",
        "harvest": "জুলাই ২০২৬",
        "story": "দেশি লাল ও মাঘী সরিষা দানা রোদে শুকিয়ে কাঠের ঘানিতে ঠাণ্ডা তাপমাত্রায় (Cold-Pressed) ভাঙানো খাঁটি ও ঝাঁঝালো সরিষার তেল। এর ফলে সরিষার সমস্ত প্রাকৃতিক ভিটামিন ও খনিজ উপাদান অক্ষত থাকে। রান্নায় আনে ঐতিহ্যবাহী আসল ঘ্রাণ ও ঝাঁঝ।",
        "benefits": [
            "কাঠের ঘানিতে ভাঙানো খাঁটি ও নির্ভেজাল সরিষার তেল।",
            "কোনো সস্তা পাম বা সয়াবিন তেল মেশানো হয়নি।",
            "শরীরের রক্ত সঞ্চালন বাড়াতে ও রান্নায় খাঁটি স্বাদ আনতে সেরা।"
        ],
        "storage": "সরাসরি সূর্যের আলো থেকে দূরে অন্ধকার ও শুষ্ক বোতলে মুখ বন্ধ করে রাখুন।",
        "img": "comingsoon.png"
    },
    "rice-atop": {
        "title": "উপকূলের খাঁটি আতপ চাল",
        "subcategory": "দেশি চাল ও শস্য",
        "category": "grain",
        "price": "আসন্ন (স্টক আউট)",
        "weight": "৫ কেজি",
        "location": "শ্যামনগর ধানের মাঠ",
        "harvest": "ডিসেম্বর ২০২৬",
        "story": "উপকূলের উর্বর পলল মাটিতে কোনো ক্ষতিকর ইউরিয়া পলিশ বা কৃত্রিম রঙ ছাড়াই প্রাকৃতিকভাবে উৎপাদিত উর্বর ও সুগন্ধি দেশি আতপ চাল। (শীতকালীন নতুন চাল আসার পর স্টক করা হবে)",
        "benefits": [
            "উপকূলের খাঁটি উর্বর মাটির উৎপাদন।",
            "পলিশ ছাড়া সম্পূর্ণ লালচে চালের ফাইবার ও পুষ্টিগুণ অটুট।",
            "পিঠা, পায়েস ও দেশি খিচুড়ি রান্নার জন্য সেরা সুগন্ধি চাল।"
        ],
        "storage": "শুষ্ক ও বায়ুরোধী বয়ামে রাখুন যাতে চালের পোকা না ধরে। মাঝে মাঝে রোদে শুকিয়ে নিতে পারেন।",
        "img": "rice.png"
    }
};

document.addEventListener('DOMContentLoaded', () => {
    // 1. Mobile Navbar Toggle
    const mobileMenuBtn = document.querySelector('.mobile-menu-btn');
    const navMenu = document.querySelector('.nav-menu');

    if (mobileMenuBtn && navMenu) {
        mobileMenuBtn.addEventListener('click', () => {
            navMenu.classList.toggle('active');
        });
        
        // Close menu when clicking outside
        document.addEventListener('click', (e) => {
            if (!e.target.closest('.nav-menu') && !e.target.closest('.mobile-menu-btn')) {
                navMenu.classList.remove('active');
            }
        });
    }

    // 2. Nav Link Active State Management
    let currentPage = window.location.pathname.split('/').pop() || 'index.html';
    currentPage = currentPage.split('?')[0].split('#')[0]; // Strip query and hashes

    const navLinks = document.querySelectorAll('.nav-link');
    navLinks.forEach(link => {
        const href = link.getAttribute('href');
        if (href === currentPage) {
            link.classList.add('active');
        } else {
            link.classList.remove('active');
        }
    });

    const bottomNavLinks = document.querySelectorAll('.bottom-nav-link');
    bottomNavLinks.forEach(link => {
        const href = link.getAttribute('href');
        if (href === currentPage) {
            link.classList.add('active');
        } else {
            link.classList.remove('active');
        }
    });

    // 3. Render Products on Products Page dynamically
    const productsGrid = document.getElementById('all-products-grid');
    if (productsGrid) {
        renderProducts('all');
    }

    function renderProducts(filterCategory) {
        productsGrid.innerHTML = '';
        let count = 1;
        for (const key in window.PRODUCT_DB) {
            const prod = window.PRODUCT_DB[key];
            if (filterCategory === 'all' || prod.category === filterCategory) {
                productsGrid.innerHTML += `
                    <div class="crate-product-card product-item" data-category="${prod.category}">
                        <div class="crate-top-banner">
                            <span>CRATE #SH-00${count}</span>
                            <span style="color: ${prod.price.includes('আসন্ন') ? '#F39C12' : '#64dd17'};">
                                ● ${prod.price.includes('আসন্ন') ? 'আসন্ন' : 'ইন স্টক'}
                            </span>
                        </div>
                        <div class="crate-image-wrapper">
                            <span class="crate-tag">${prod.subcategory}</span>
                            <img src="${prod.img}" alt="${prod.title}">
                        </div>
                        <div class="crate-body">
                            <div class="crate-meta">
                                <span>📍 ${prod.location}</span>
                                <span>📅 ${prod.harvest}</span>
                            </div>
                            <h3 class="crate-title">${prod.title}</h3>
                            <p class="crate-story">${prod.story.substring(0, 100)}...</p>
                            <div class="crate-bottom">
                                <div class="crate-price-wrapper">
                                    <span class="crate-price-label">মূল্য</span>
                                    <span class="crate-price">${prod.price}</span>
                                </div>
                                <span class="crate-weight">${prod.weight}</span>
                            </div>
                            <div class="crate-actions">
                                <a href="tel:+8801873520181" class="btn btn-primary" style="padding: 10px;">📞 কল করুন</a>
                                <button class="btn btn-outline view-details-btn" data-product="${key}" style="padding: 10px;">বিস্তারিত</button>
                            </div>
                        </div>
                    </div>
                `;
                count++;
            }
        }
        
        // Bind dynamic details button click
        bindDetailButtons();
    }

    // 4. Products Filtering Logic (Pure Frontend)
    const filterChips = document.querySelectorAll('.filter-chip');
    filterChips.forEach(chip => {
        chip.addEventListener('click', () => {
            filterChips.forEach(c => c.classList.remove('active'));
            chip.classList.add('active');
            const filterValue = chip.getAttribute('data-filter');

            // If we are on products page, render dynamically
            if (productsGrid) {
                renderProducts(filterValue);
            } else {
                // If we are on Home page, toggle CSS class visibility
                const staticProducts = document.querySelectorAll('.product-item');
                staticProducts.forEach(item => {
                    const itemCat = item.getAttribute('data-category');
                    if (filterValue === 'all' || itemCat === filterValue) {
                        item.style.display = 'flex';
                    } else {
                        item.style.display = 'none';
                    }
                });
            }
        });
    });

    // 5. Product Detail Modal/Drawer Control
    const productModal = document.getElementById('productModal');
    
    function bindDetailButtons() {
        const detailButtons = document.querySelectorAll('.view-details-btn');
        if (detailButtons.length > 0 && productModal) {
            const modalImg = document.getElementById('modalImg');
            const modalTitle = document.getElementById('modalTitle');
            const modalSubcategory = document.getElementById('modalSubcategory');
            const modalPrice = document.getElementById('modalPrice');
            const modalDesc = document.getElementById('modalDesc');
            const modalLocation = document.getElementById('modalLocation');
            const modalHarvest = document.getElementById('modalHarvest');
            const modalDetailLink = document.getElementById('modalDetailLink');
            
            detailButtons.forEach(btn => {
                btn.addEventListener('click', (e) => {
                    e.preventDefault();
                    const prodId = btn.getAttribute('data-product');
                    const product = window.PRODUCT_DB[prodId];
                    
                    if (product) {
                        modalImg.src = product.img;
                        modalImg.alt = product.title;
                        modalTitle.textContent = product.title;
                        modalSubcategory.textContent = product.subcategory;
                        modalPrice.textContent = `${product.price} / ${product.weight}`;
                        modalDesc.textContent = product.story;
                        modalLocation.textContent = `📍 ${product.location}`;
                        modalHarvest.textContent = `📅 সংগ্রহ: ${product.harvest}`;
                        modalDetailLink.href = `product-detail.html?id=${prodId}`;
                        
                        productModal.classList.add('active');
                        document.body.style.overflow = 'hidden'; // Lock scrolling
                    }
                });
            });
        }
    }
    
    // Initial binding for home page buttons
    bindDetailButtons();

    // Close Modal Logic
    const closeModalBtn = document.getElementById('closeModal');
    if (closeModalBtn && productModal) {
        const closeModal = () => {
            productModal.classList.remove('active');
            document.body.style.overflow = '';
        };

        closeModalBtn.addEventListener('click', closeModal);
        productModal.addEventListener('click', (e) => {
            if (e.target === productModal) {
                closeModal();
            }
        });
    }

    // 6. Testimonials horizontal slider logic
    const reviewsTrack = document.getElementById('reviewsTrack');
    if (reviewsTrack) {
        let isDown = false;
        let startX;
        let scrollLeft;

        reviewsTrack.addEventListener('mousedown', (e) => {
            isDown = true;
            startX = e.pageX - reviewsTrack.offsetLeft;
            scrollLeft = reviewsTrack.scrollLeft;
            reviewsTrack.style.cursor = 'grabbing';
        });
        reviewsTrack.addEventListener('mouseleave', () => {
            isDown = false;
            reviewsTrack.style.cursor = 'grab';
        });
        reviewsTrack.addEventListener('mouseup', () => {
            isDown = false;
            reviewsTrack.style.cursor = 'grab';
        });
        reviewsTrack.addEventListener('mousemove', (e) => {
            if (!isDown) return;
            e.preventDefault();
            const x = e.pageX - reviewsTrack.offsetLeft;
            const walk = (x - startX) * 2; // Scroll multiplier
            reviewsTrack.scrollLeft = scrollLeft - walk;
        });

        // Touch support for mobile
        reviewsTrack.addEventListener('touchstart', (e) => {
            isDown = true;
            startX = e.touches[0].pageX - reviewsTrack.offsetLeft;
            scrollLeft = reviewsTrack.scrollLeft;
        });
        reviewsTrack.addEventListener('touchend', () => {
            isDown = false;
        });
        reviewsTrack.addEventListener('touchmove', (e) => {
            if (!isDown) return;
            const x = e.touches[0].pageX - reviewsTrack.offsetLeft;
            const walk = (x - startX) * 1.5;
            reviewsTrack.scrollLeft = scrollLeft - walk;
        });
        
        // Auto scroll loops
        let direction = 1;
        setInterval(() => {
            if (!isDown) {
                reviewsTrack.scrollLeft += direction * 1;
                // If hit boundary, reverse
                if (reviewsTrack.scrollLeft >= (reviewsTrack.scrollWidth - reviewsTrack.clientWidth - 10)) {
                    direction = -1;
                } else if (reviewsTrack.scrollLeft <= 5) {
                    direction = 1;
                }
            }
        }, 30);
    }

    // 7. Statistics Count Up logic using Intersection Observer
    const statNumbers = document.querySelectorAll('.stat-number');
    if (statNumbers.length > 0) {
        const countUp = (el) => {
            const target = parseInt(el.getAttribute('data-target'));
            let current = 0;
            const step = target > 1000 ? Math.ceil(target / 100) : Math.ceil(target / 50);
            const duration = 2000; // 2s
            const intervalTime = Math.min(Math.ceil(duration / (target / step)), 30);
            
            const timer = setInterval(() => {
                current += step;
                if (current >= target) {
                    current = target;
                    clearInterval(timer);
                }
                
                // Add suffix formatting
                if (target === 100) {
                    el.textContent = `${current}%`;
                } else if (target === 5000) {
                    el.textContent = `${current}+`;
                } else if (target === 120) {
                    el.textContent = `${current}+`;
                } else {
                    el.textContent = current;
                }
            }, intervalTime);
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    countUp(entry.target);
                    observer.unobserve(entry.target);
                }
            });
        }, { threshold: 0.5 });

        statNumbers.forEach(num => observer.observe(num));
    }

    // 8. Story Timeline scroll reveal
    const timelineItems = document.querySelectorAll('.timeline-item');
    if (timelineItems.length > 0) {
        const revealTimeline = () => {
            timelineItems.forEach(item => {
                const itemTop = item.getBoundingClientRect().top;
                const windowHeight = window.innerHeight;
                if (itemTop < windowHeight * 0.85) {
                    item.classList.add('revealed');
                }
            });
        };
        
        window.addEventListener('scroll', revealTimeline);
        revealTimeline(); // Trigger once on load
    }

    // 9. FAQ Accordions toggle in faq.html and index.html
    const faqAccordions = document.querySelectorAll('.faq-accordion-header');
    faqAccordions.forEach(header => {
        header.addEventListener('click', () => {
            const item = header.parentElement;
            item.classList.toggle('active');
        });
    });
});
